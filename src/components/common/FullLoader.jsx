import React from 'react';

const Loader = () => {
    return (
        <div className="full_loader_container">
              <span className="loader"></span>
        </div>
    );
};

export default Loader;